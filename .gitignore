# raw and cleaned GPX carry timestamps: where you were, when. Never commit them.
*.gpx
raw/*
!raw/README.md

# build output: the zip for wp-admin upload. The plugins/ folders themselves are committed.
dist/

.DS_Store
__pycache__/
